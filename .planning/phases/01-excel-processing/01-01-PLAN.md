---
phase: 01-excel-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/__init__.py
  - src/core/__init__.py
  - tests/__init__.py
  - .gitignore
autonomous: true
requirements:
  - EXCL-01
must_haves:
  truths:
    - Project has proper directory structure for Python app
    - All required dependencies are listed in requirements.txt
    - Package structure supports imports like `from src.core import ExcelReader`
    - .gitignore excludes sensitive files (Excel, config)
  artifacts:
    - path: requirements.txt
      provides: Project dependencies (openpyxl, xlrd, requests, pyinstaller)
      contains: "openpyxl>=3.1.2"
    - path: src/__init__.py
      provides: Source package root
      contains: "# Excel data push assistant"
    - path: src/core/__init__.py
      provides: Core module package
      contains: "# Core business logic"
    - path: tests/__init__.py
      provides: Test package root
      contains: "# Tests"
  key_links:
    - from: src/core/__init__.py
      to: src/__init__.py
      via: Python package hierarchy
---

<objective>
Set up the project structure, dependencies, and package initialization for the Excel data push assistant.

Purpose: Create the foundation that all subsequent plans depend on.
Output: requirements.txt, src/ package structure, tests/ directory, .gitignore
</objective>

<execution_context>
@/Users/zhangtao/Work/py/scan_excel/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-excel-processing/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project directory structure</name>
  <files>
    - src/__init__.py
    - src/core/__init__.py
    - src/utils/__init__.py
    - tests/__init__.py
    - assets/
  </files>
  <action>
    Create the directory structure for a Python application:
    - `src/` - Main source code package
    - `src/core/` - Core business logic modules (excel_reader, formatter)
    - `src/utils/` - Utility helpers
    - `tests/` - Test files
    - `assets/` - Icons and resources

    Each __init__.py should contain a brief module docstring. Follow the structure from ARCHITECTURE.md.
  </action>
  <verify>
    <automated>ls -la src/core/ src/utils/ tests/ assets/</automated>
  </verify>
  <done>
    All directories exist with __init__.py files containing docstrings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt with locked dependencies</name>
  <files>requirements.txt</files>
  <action>
    Create requirements.txt with exact versions per D-01 (STACK.md research):
    - openpyxl>=3.1.2 (for .xlsx files)
    - xlrd>=2.0.1 (for .xls files)
    - requests>=2.31.0 (for webhook calls in Phase 2)
    - pyinstaller>=6.0.0 (for Phase 5 packaging)

    Also add pytest for testing. Format: one package per line with version constraints.
  </action>
  <verify>
    <automated>grep -E "^openpyxl|^xlrd|^requests|^pyinstaller" requirements.txt</automated>
  </verify>
  <done>
    requirements.txt contains all 4 core dependencies with version constraints matching STACK.md
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore to exclude sensitive files</name>
  <files>.gitignore</files>
  <action>
    Create or update .gitignore to exclude:
    - Excel files: *.xlsx, *.xls (per PROJECT.md data privacy constraint)
    - Config files: config.json, config.yaml (per PROJECT.md webhook security)
    - Python build artifacts: __pycache__/, *.pyc, build/, dist/
    - IDE files: .idea/, .vscode/
    - Virtual env: venv/, .venv/
    - PyInstaller: *.spec (except app.spec if committed)

    Use standard Python gitignore patterns.
  </action>
  <verify>
    <automated>grep -E "^\*\.xlsx|^\*\.xls|^config\." .gitignore | head -5</automated>
  </verify>
  <done>
    .gitignore contains patterns for *.xlsx, *.xls, and config files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file→app | Untrusted Excel file input from user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore | mitigate | Per D-15 and PROJECT.md, webhook config and Excel files excluded from git |
</threat_model>

<verification>
1. Directory structure matches ARCHITECTURE.md: `ls -R src/ tests/`
2. requirements.txt has all dependencies: `cat requirements.txt | wc -l` >= 4
3. .gitignore excludes sensitive files: `grep "*.xlsx" .gitignore`
</verification>

<success_criteria>
- Project can be installed: `pip install -r requirements.txt` succeeds
- Import structure works: `python -c "from src.core import excel_reader"` (file may not exist yet but import path is valid)
- Sensitive files protected: `git check-ignore *.xlsx` returns match
</success_criteria>

<output>
After completion, create `.planning/phases/01-excel-processing/01-01-SUMMARY.md`
</output>
