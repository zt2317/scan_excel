---
phase: 01-excel-processing
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
  - 01-03-PLAN.md
files_modified:
  - src/core/formatter.py
  - src/core/preview.py
  - tests/test_formatter.py
  - tests/test_preview.py
  - src/core/__init__.py
autonomous: true
requirements:
  - EXCL-04
  - MD-01
  - MD-02
  - MD-03
must_haves:
  truths:
    - Generates valid Markdown tables per D-10, MD-01
    - Bold headers per D-11, MD-02
    - Dates formatted as YYYY-MM-DD per D-07, MD-03
    - Shows data preview (first 10 rows) per D-12, EXCL-04
  artifacts:
    - path: src/core/formatter.py
      provides: Markdown table generation
      contains: "class MarkdownFormatter"
    - path: src/core/preview.py
      provides: Data preview functionality
      contains: "def generate_preview"
    - path: tests/test_formatter.py
      provides: Formatter unit tests
    - path: tests/test_preview.py
      provides: Preview unit tests
  key_links:
    - from: MarkdownFormatter.format()
      to: aligned Markdown table output
      via: table formatting algorithm
    - from: preview.generate_preview()
      to: first 10 rows selection
      via: data slicing
---

<objective>
Implement MarkdownFormatter for table generation and preview functionality per D-10, D-11, D-12.

Purpose: Core functionality for EXCL-04 (data preview), MD-01~03 (Markdown formatting).
Output: formatter.py, preview.py with tests
</objective>

<execution_context>
@/Users/zhangtao/Work/py/scan_excel/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-excel-processing/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MarkdownFormatter with aligned tables</name>
  <files>src/core/formatter.py</files>
  <action>
    Create MarkdownFormatter class per D-10, D-11, MD-01, MD-02:

    ```python
    from typing import List, Dict, Any
    from datetime import datetime

    class MarkdownFormatter:
        """Format data as Markdown tables per D-10, D-11."""

        # Per D-11: Header display names (Chinese)
        COLUMN_DISPLAY = {
            'date': '日期',
            'shipper': '发货人',
            'tracking': '提单号',
            'inbound_pending': '入库未扫',
            'outbound_pending': '出库未扫'
        }

        def format(self, data: List[Dict[str, Any]]) -> str:
            """Format data as Markdown table.

            Per D-10: Aligned table format
            Per D-11: Bold headers
            Per MD-01: Standard Markdown table
            Per MD-02: Bold headers
            Per MD-03: Date formatted as YYYY-MM-DD
            """
            pass

        def _format_date(self, value: Any) -> str:
            """Format date to YYYY-MM-DD per D-07, MD-03.

            Per D-07 (the agent's Discretion): Support multiple input formats:
            - 2024/1/15, 2024-01-15, 15/01/2024, 15-Jan-2024
            """
            pass

        def _calculate_column_widths(self, data: List[Dict[str, Any]]) -> Dict[str, int]:
            """Calculate column widths for alignment per D-10."""
            pass
    ```

    Output format example:
    ```markdown
    | **日期** | **发货人** | **提单号** | **入库未扫** | **出库未扫** |
    |----------|------------|------------|--------------|--------------|
    | 2024-01-15 | 张三 | BL001 | 5 | - |
    ```

    Implementation notes:
    - Use | as delimiter with leading/trailing |
    - Header separator: |---|---|...|
    - Bold headers: **header**
    - Left-aligned by default (most readable for Chinese)
    - Date parsing: try multiple formats, fallback to original string
  </action>
  <verify>
    <automated>grep -E "class MarkdownFormatter|def format|def _format_date|COLUMN_DISPLAY" src/core/formatter.py</automated>
  </verify>
  <done>
    MarkdownFormatter class exists with format() and date formatting methods
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement preview generator for first 10 rows</name>
  <files>src/core/preview.py</files>
  <action>
    Create preview functionality per D-12, EXCL-04:

    ```python
    from typing import List, Dict, Any

    def generate_preview(data: List[Dict[str, Any]], max_rows: int = 10) -> str:
        """Generate preview of first N rows.

        Per D-12: Show first 10 rows for user confirmation
        Per EXCL-04: Data preview functionality
        """
        pass

    def format_preview_table(data: List[Dict[str, Any]], max_rows: int = 10) -> str:
        """Format preview as simple text table (not Markdown).

        Used for GUI display before sending.
        """
        pass
    ```

    Preview should be human-readable text table, not Markdown (which is for sending).
    Simple format:
    ```
    日期       | 发货人 | 提单号 | 入库未扫 | 出库未扫
    -----------|--------|--------|----------|----------
    2024-01-15 | 张三   | BL001  | 5        | -
    ```

    Implementation:
    - Slice data to max_rows
    - Format as fixed-width text table
    - Truncate long values if needed
  </action>
  <verify>
    <automated>grep -E "def generate_preview|def format_preview_table" src/core/preview.py</automated>
  </verify>
  <done>
    generate_preview() function exists with max_rows=10 default
  </done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for formatter and preview</name>
  <files>
    - tests/test_formatter.py
    - tests/test_preview.py
  </files>
  <action>
    Create test_formatter.py:

    ```python
    import pytest
    from src.core.formatter import MarkdownFormatter

    class TestMarkdownFormatter:
        def test_format_basic_table(self):
            """Test basic Markdown table generation per MD-01."""
            pass

        def test_bold_headers(self):
            """Test bold headers per D-11, MD-02."""
            # Assert headers wrapped in **
            pass

        def test_date_formatting(self):
            """Test date format YYYY-MM-DD per MD-03."""
            # Test inputs: 2024/1/15, 2024-01-15, 15/01/2024
            # All should output: 2024-01-15
            pass

        def test_null_display(self):
            """Test null values shown as -."""
            pass

        def test_empty_data(self):
            """Test handling of empty data."""
            pass
    ```

    Create test_preview.py:

    ```python
    import pytest
    from src.core.preview import generate_preview

    class TestPreview:
        def test_preview_first_10_rows(self):
            """Test preview shows first 10 rows per D-12."""
            data = [{'date': f'2024-01-{i:02d}'} for i in range(1, 20)]
            preview = generate_preview(data)
            # Assert only 10 rows in preview
            pass

        def test_preview_custom_count(self):
            """Test preview with custom row count."""
            pass

        def test_preview_empty(self):
            """Test preview with empty data."""
            pass
    ```
  </action>
  <verify>
    <automated>grep -E "def test_" tests/test_formatter.py tests/test_preview.py | wc -l</automated>
  </verify>
  <done>
    At least 5 tests in test_formatter.py and 3 tests in test_preview.py
  </done>
</task>

<task type="auto">
  <name>Task 4: Export public API from core module</name>
  <files>src/core/__init__.py</files>
  <action>
    Update src/core/__init__.py to export the public API:

    ```python
    """Core business logic for Excel data push assistant.

    This module contains:
    - ExcelReader: Read .xlsx and .xls files
    - ColumnDetector: Detect target columns with fuzzy matching
    - MarkdownFormatter: Format data as Markdown tables
    - preview: Generate data previews
    """

    from .excel_reader import ExcelReader
    from .column_detector import ColumnDetector
    from .formatter import MarkdownFormatter
    from .preview import generate_preview

    __all__ = [
        'ExcelReader',
        'ColumnDetector',
        'MarkdownFormatter',
        'generate_preview',
    ]
    ```

    This enables clean imports: `from src.core import ExcelReader, ColumnDetector`
  </action>
  <verify>
    <automated>grep -E "from .excel_reader|from .column_detector|from .formatter|__all__" src/core/__init__.py</automated>
  </verify>
  <done>
    All 3 classes and preview function exported in __all__
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data→formatter | Processed data → Markdown output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | Markdown output | accept | Data is meant to be shared via webhook per design |
| T-01-07 | Denial of Service | Table generation | accept | No complex processing, simple string formatting |
</threat_model>

<verification>
1. Import works: `python -c "from src.core import MarkdownFormatter, generate_preview"`
2. Tests pass: `pytest tests/test_formatter.py tests/test_preview.py -v`
3. Date formatting: 2024/1/15 → 2024-01-15
4. Preview limits: 20 rows input → 10 rows output
</verification>

<success_criteria>
- pytest tests/test_formatter.py and tests/test_preview.py pass
- MarkdownFormatter generates valid Markdown tables with **bold** headers
- Date formatting converts various formats to YYYY-MM-DD
- Preview function returns first 10 rows max
- Public API exports work: `from src.core import ExcelReader, ColumnDetector, MarkdownFormatter`
</success_criteria>

<output>
After completion, create `.planning/phases/01-excel-processing/01-04-SUMMARY.md`
</output>
