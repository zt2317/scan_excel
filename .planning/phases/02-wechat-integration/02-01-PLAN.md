---
phase: 02-wechat-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/config.py
  - config/.gitkeep
  - tests/test_config.py
  - .gitignore
autonomous: true
requirements:
  - WX-01
  - WX-02
must_haves:
  truths:
    - Configuration can be saved to local JSON file
    - Configuration can be loaded from local JSON file
    - Webhook URL is persisted across application restarts
    - Config files are excluded from git via .gitignore
  artifacts:
    - path: src/core/config.py
      provides: ConfigStore class with load/save methods
      exports: [ConfigStore, ConfigError]
    - path: config/webhook_config.json
      provides: Persistent configuration storage
      contains: webhook_url, last_folder
    - path: tests/test_config.py
      provides: Unit tests for config module
      min_tests: 5
  key_links:
    - from: src/core/config.py
      to: config/webhook_config.json
      via: pathlib Path operations
      pattern: Path.*config.*json
---

<objective>
Implement configuration storage module for webhook URL persistence.

Purpose: Enable users to save and retrieve their enterprise WeChat webhook URL without exposing it in version control.
Output: ConfigStore class with JSON-based persistence, protected by .gitignore
</objective>

<execution_context>
@/Users/zhangtao/Work/py/scan_excel/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-wechat-integration/02-CONTEXT.md
@.planning/phases/02-wechat-integration/02-RESEARCH.md
@.planning/REQUIREMENTS.md

## Key Decisions to Implement
- D-01: Store in `config/` folder under project directory
- D-02: Use JSON format
- D-03: Plain text storage with .gitignore protection
- D-04: Store webhook_url and optional last_folder

## Existing Code Patterns
- Phase 1 uses `src/core/` for core modules
- Chinese error messages (延续Phase 1风格)
- Direct error handling without complex fallbacks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore for config directory</name>
  <files>.gitignore</files>
  <action>
    Add entries to .gitignore to protect configuration files:
    - config/webhook_config.json
    - config/*.json
    
    Ensure the entries are clearly commented:
    ```
    # Phase 2: Webhook configuration (contains sensitive data)
    config/webhook_config.json
    config/*.json
    ```
  </action>
  <verify>
    <automated>grep -q "webhook_config.json" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.gitignore contains config/webhook_config.json exclusion</done>
</task>

<task type="auto">
  <name>Task 2: Create config directory structure</name>
  <files>config/.gitkeep</files>
  <action>
    Create the config directory with a .gitkeep file to ensure the directory is tracked but config files are not:
    ```
    mkdir -p config
    echo "# This directory stores local configuration files" > config/.gitkeep
    echo "# webhook_config.json is excluded from git - see .gitignore" >> config/.gitkeep
    ```
  </action>
  <verify>
    <automated>test -d config && test -f config/.gitkeep && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>config/ directory exists with .gitkeep file</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ConfigStore class</name>
  <files>src/core/config.py</files>
  <behavior>
    - Test 1: load() returns empty dict when config file doesn't exist
    - Test 2: save() creates config file with correct JSON structure
    - Test 3: load() returns previously saved configuration
    - Test 4: save() overwrites existing config file
    - Test 5: ConfigError raised for invalid JSON
  </behavior>
  <action>
    Create src/core/config.py with ConfigStore class implementing:
    
    1. ConfigStore class:
       - __init__(project_root=None): Initialize with optional project root
       - load(): Load config from JSON, return empty dict if not exists
       - save(config): Save config dict to JSON file
       - get_webhook_url(): Convenience method to get webhook_url
       - set_webhook_url(url): Convenience method to set webhook_url
    
    2. ConfigError exception class:
       - Custom exception for configuration errors
    
    3. Implementation details:
       - Use pathlib.Path for cross-platform path handling
       - Use UTF-8 encoding with ensure_ascii=False for Chinese text
       - Pretty-print JSON with indent=2
       - Auto-create config directory on save
       - Handle JSONDecodeError gracefully with ConfigError
    
    4. Error messages (Chinese, 延续Phase 1风格):
       - "配置文件读取失败" for read errors
       - "配置文件格式错误" for JSON decode errors
       - "配置保存失败" for write errors
  </action>
  <verify>
    <automated>python -c "from src.core.config import ConfigStore, ConfigError; print('PASS')" 2>&1 || echo "FAIL"</automated>
  </verify>
  <done>src/core/config.py exists with ConfigStore class and ConfigError exception</done>
</task>

<task type="auto">
  <name>Task 4: Create unit tests for config module</name>
  <files>tests/test_config.py</files>
  <action>
    Create comprehensive unit tests for the config module:
    
    1. Test cases:
       - test_load_nonexistent: Returns empty dict when file doesn't exist
       - test_save_and_load: Roundtrip save and load
       - test_get_set_webhook_url: Convenience methods work
       - test_overwrite_existing: Can update existing config
       - test_invalid_json: Raises ConfigError on corrupted file
       - test_chinese_content: Handles Chinese text correctly
    
    2. Test utilities:
       - Use tempfile.TemporaryDirectory for test isolation
       - Clean up test files after each test
    
    3. Run tests and ensure all pass
  </action>
  <verify>
    <automated>cd /Users/zhangtao/Work/py/scan_excel && python -m pytest tests/test_config.py -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>All tests in test_config.py pass (at least 6 tests)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file ↔ Git | webhook_url must never cross into version control |
| Config file ↔ User | Plain text storage is acceptable for this use case (low sensitivity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | config/webhook_config.json | mitigate | .gitignore excludes config files; ConfigStore uses project-local path |
| T-02-02 | Tampering | config/webhook_config.json | accept | Plain text is acceptable; encryption deferred to v2 per CONTEXT.md |
| T-02-03 | Denial of Service | ConfigStore.load() | mitigate | JSONDecodeError caught and raised as ConfigError with clear message |
</threat_model>

<verification>
- ConfigStore can save and load webhook URL
- config/ directory is tracked in git with .gitkeep
- config/*.json files are excluded via .gitignore
- All unit tests pass
- Chinese error messages are clear and consistent with Phase 1 style
</verification>

<success_criteria>
- ✓ ConfigStore class implemented with load/save/get/set methods
- ✓ Configuration persists across application restarts
- ✓ .gitignore properly excludes config/webhook_config.json
- ✓ Unit tests cover all major scenarios (6+ tests)
- ✓ Error handling follows Phase 1 patterns (Chinese messages, direct errors)
</success_criteria>

<output>
After completion, create `.planning/phases/02-wechat-integration/02-01-SUMMARY.md`
</output>
